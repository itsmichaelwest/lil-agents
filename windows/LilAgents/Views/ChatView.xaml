<UserControl
    x:Class="LilAgents.Views.ChatView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Override TextBox to have no chrome in any state -->
        <Style x:Key="ChromelessTextBox" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="6,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer
                                x:Name="ContentElement"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden"
                                Margin="{TemplateBinding Padding}" />
                            <TextBlock
                                x:Name="PlaceholderTextContentPresenter"
                                Text="{TemplateBinding PlaceholderText}"
                                Foreground="{Binding PlaceholderForeground, RelativeSource={RelativeSource TemplatedParent}}"
                                Margin="{TemplateBinding Padding}"
                                IsHitTestVisible="False"
                                Visibility="Collapsed" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid" RowDefinitions="*,Auto">
        <!-- Chat message area -->
        <ScrollViewer
            x:Name="ChatScroller"
            Grid.Row="0"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Disabled"
            Padding="10,4,10,4">
            <StackPanel x:Name="MessagesPanel" Spacing="0" />
        </ScrollViewer>

        <!-- Separator + input area -->
        <StackPanel Grid.Row="1" Spacing="0">
            <Border
                x:Name="InputSeparator"
                Height="1"
                HorizontalAlignment="Stretch" />

            <Grid Padding="10,6,10,8">
                <TextBox
                    x:Name="InputBox"
                    Style="{StaticResource ChromelessTextBox}"
                    PlaceholderText="Ask Claude..."
                    AcceptsReturn="False"
                    KeyDown="InputBox_KeyDown" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
